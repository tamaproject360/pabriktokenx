version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: pabriktokenx-backend
    restart: unless-stopped
    ports:
      - "9999:9999"
    volumes:
      # Mount docker-specific config file
      - ./config.docker.yaml:/app/config.yaml
      # Mount auth directory (persistent)
      - ./auths:/app/auths
      # Mount logs directory (persistent)
      - ./logs:/app/logs
      # Mount assets directory (persistent)
      - ./assets:/app/assets
      # Optional: Mount model settings
      - ./model_settings.json:/app/model_settings.json:ro
    environment:
      - TZ=Asia/Jakarta
      - MANAGEMENT_PASSWORD=${MANAGEMENT_PASSWORD:-}
      - OAUTH_PUBLIC_BASE_URL=${OAUTH_PUBLIC_BASE_URL:-}
      - GEMINI_OAUTH_CLIENT_ID=${GEMINI_OAUTH_CLIENT_ID:-}
      - GEMINI_OAUTH_CLIENT_SECRET=${GEMINI_OAUTH_CLIENT_SECRET:-}
      - GEMINI_EMBEDDING_API_KEY=${GEMINI_EMBEDDING_API_KEY:-}
      - GEMINI_EMBEDDING_MODEL=gemini-embedding-001
      - GEMINI_EMBEDDING_TASK_TYPE=RETRIEVAL_DOCUMENT
      - OLLAMA_EMBEDDING_URL=http://192.168.0.25:11434/api/embed
      - OLLAMA_EMBEDDING_MODEL=nomic-embed-text
    networks:
      - pabriktokenx-network
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:9999/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
    container_name: pabriktokenx-frontend
    restart: unless-stopped
    ports:
      - "8686:80"
    depends_on:
      - backend
    environment:
      - TZ=Asia/Jakarta
    networks:
      - pabriktokenx-network
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://127.0.0.1:80"]
      interval: 30s
      timeout: 10s
      retries: 3

networks:
  pabriktokenx-network:
    driver: bridge

volumes:
  # Define named volumes if you prefer
  auths-data:
  logs-data:
  assets-data:
